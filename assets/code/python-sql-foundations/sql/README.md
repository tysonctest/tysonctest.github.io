# SQL Foundations Code Samples

This folder contains cleaned SQL examples based on introductory SQL practice with a relational sample database structure. The examples demonstrate SELECT statements, filtering, sorting, aggregation, joins, and business-style summary queries.

The examples are included to document foundational SQL fluency; they are not tied to private data.
